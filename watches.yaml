---
# Use the 'create api' subcommand to add watches to this file.
- version: v1alpha1
  group: weather.aputtur.com
  kind: CityWeather
  playbook: playbooks/cityweather.yml
# +kubebuilder:scaffold:watch
